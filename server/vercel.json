{
  "version": 2,
  "builds": [
    {
      "src": "*.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "/" }],

  "installCommand": "npm i multer-gridfs-storage --legacy-peer-deps"
}
